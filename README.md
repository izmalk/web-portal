# Web-portal

Personal web portal. Built with Hugo.

To build and serve via local web-server:

```shell
hugo serve
```

To build HTML for publishing:

```shell
hugo
```

To publish: just upload the contents of the `public` folder to your web server.
